export interface SideBarProps {
	initialWidth: number;
	minWidth: number;
	maxWidth: number;
	sideBarWidth: number;
	setSidebarWidth: any;
	isResizing: boolean;
	setIsResizing: any;
}

export interface MaterialPreviewProps {
	sideBarWidth: number;
}
